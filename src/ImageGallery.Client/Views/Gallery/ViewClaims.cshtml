@model Dictionary<string,string>
@{
    ViewData["Title"] = "View";
}

<h3>Tokens</h3>
<ul>

    @foreach (var key in Model.Keys)
    {
        <li> @key - <blockquote>@Model[key].ToString()</blockquote></li>

    }
</ul>
<h3>View Claims</h3>
<ul>
    @foreach (var claim in User.Claims)
    {
        <li><b>@claim.Type</b>--- @claim.Value</li>
    }
</ul>


